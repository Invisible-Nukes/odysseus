# 2026-07-08 Plan — Launcher UX, Hermes Shutdown, Downloads & Crashes

Status: fixes applied + verified; **awaiting commit/push** (P6).
Scope: `launch-windows.ps1`, `launch.bat`, `scripts/install_ollama.ps1`, `reset.ps1`, `routes/cookbook_routes.py`.

---

## 1) Completed today (COMMITTED earlier)
### P1 — First-launch admin credential prompt (DONE, `4f11c91`)
- Setup.py runs interactively (no auto-generated password) when `data/auth.json` absent.
- Skips prompt on subsequent runs via `ODYSSEUS_SKIP_ADMIN_PROMPT=1`.

### P2 — launch.bat warning + confirm gate (DONE, `eb3d9c3`)
- Credential-setup warning + confirm gate before launch; trailing `pause` removed.

### P3 — Remove blocking Enter prompts; auto-open tab (DONE, `e8713a8`)
- `launch-windows.ps1` trailing `Read-Host` → 3s sleep + auto-open message.
- `launch.bat` / `scripts/install_ollama.ps1` blocking prompts removed.

---

## 2) Hermes Desktop shutdown fix (APPLIED + VERIFIED, not committed)
### P4 — Tighten process matchers (DONE)
- **Files:** `launch-windows.ps1` `Get-OdysseusProcesses` (line 81) + `reset.ps1` (line 22).
- **Change:** match only (a) cmd line containing the venv python path **and** `uvicorn app:app`, (b) venv `chroma.exe`; **exclude** any process whose executable resolves into `%LOCALAPPDATA%\hermes\hermes-agent`. Dropped bare `odysseus` + repo-root substring matches. Also fixed a PS 5.1 `Where-Object` `return`-early quirk (use `$true; return` not `return $true`).
- **Why:** prevents the launcher/reset from killing Hermes-owned processes; resolves open issue L1.

### P5 — Live verification (DONE)
- Both scripts parse clean (PS 5.1 AST).
- Behavioral test: 7/7 pass.
- Live process-list proof: both matchers catch the 2 running Odysseus server PIDs, match **0** of 19 live Hermes processes.

---

## 3) Dependency / model-download / runtime fixes (APPLIED + VERIFIED, not committed)
### P1 (downloads) — ChromaDB server never starts [DONE]
- **Problem:** `Ensure-ChromaDbPackage` only handled `chromadb-client` (HTTP client, no `chroma.exe`). Server never started → every launch logged RAG/memory/tool-index init failures.
- **Fix:** `Ensure-ChromaDbPackage` now installs the full `chromadb` package (provides `chroma.exe`) via `ODYSSEUS_PIP_MIRROR` when the server binary is missing, then returns the exe path.
- **Verification:** `pip install chromadb` via mirror → `chroma.exe` present; `chroma.exe run --host 127.0.0.1 --port 8100` → listening. launch-windows.ps1 parses clean.

### P2 (downloads) — HuggingFace downloads silent/hanging on bad repo [DONE, corrected]
- **Problem:** `POST /api/model/download` (backend=hf) shelled out to `hf download` / `snapshot_download`. A non-existent/private repo failed as a silent/hanging job with no clear message.
- **Fix:** Added `_check_hf_reachable(repo_id, hf_token)` pre-flight in `model_download` (local downloads). Probes the actual requested repo; returns clear error before launching: 401/403 → private/gated/renamed/needs-auth; 404 → repo not found; network error → don't block (let real attempt report).
- **Corrected diagnosis (control test):** HF IS reachable (`gpt2` → 200). The user's `unsloth/Qwen3.5-2B-GGUF` **EXISTS (200)** — the original failure was NOT a missing repo. An initial `HEAD .../resolve/main/config.json` probe was wrong (404 for both valid and invalid GGUF repos); replaced with `GET /api/models/{repo_id}`.
- **Verification:** `gpt2`, `Qwen/Qwen2.5-0.5B-Instruct-GGUF`, `unsloth/Qwen3.5-2B-GGUF` → not blocked; `this-repo-does-not-exist-xyz123/nope` → blocked with clear message. `routes/cookbook_routes.py` imports clean.

### P3 (downloads) — pip mirror single point of failure [DEFERRED / optional]
- If `pypi.tuna.tsinghua.edu.cn` is ever unreachable, install fails with diagnostics only. Optional future: prompt for an alternate index. Not done (mirror currently working).

---

## 4) Done criteria
1. ✅ Matchers no longer match bare `odysseus` or the repo-root path.
2. ✅ Launching Odysseus does not shut down Hermes Desktop (verified: 0 Hermes procs matched).
3. ✅ Server still matched (venv uvicorn + chroma) so stale servers are still stopped.
4. ✅ ChromaDB server starts on launch.
5. ✅ HF download returns clear error for bad/private repos.
6. ✅ Ollama downloads unchanged (work).
7. ✅ pip deps unchanged (work via mirror).
8. ⏳ **Change committed and pushed to `origin/dev` (PENDING — P6).**
9. ❓ (Optional) Repro the user's real `unsloth/Qwen3.5-2B-GGUF` download failure (repo exists; cause unconfirmed).

## Files changed (uncommitted)
- `launch-windows.ps1` — Hermes-shutdown matcher (P4) + ChromaDB full-package install (P1).
- `reset.ps1` — Hermes-shutdown matcher (P4).
- `routes/cookbook_routes.py` — `_check_hf_reachable` + pre-flight call in `model_download` (P2).
- `tests/test_cookbook_hf_reachable.py` — new test for the HF pre-flight.
- `dev_log/analysis/2026-07-08-analysis.md`, `dev_log/plans/2026-07-08-plan.md`, `dev_log/sessions/2026-07-08-session.md` — logs.
