# 2026-07-10 Plan — Sync Fork with Upstream (pewdiepie-archdaemon/odysseus)

Status: done (merged, tested, pushed to `origin/dev`).
Scope: bring our `dev` branch current with `upstream/dev` (was 44 commits behind).

---

## 1) Pre-merge assessment (DONE)
- `git fetch upstream`; measured divergence: **44 behind / 49 ahead** vs `upstream/dev`.
- Dry, aborted merge (`git merge --no-commit` then `--abort`) to map real conflicts:
  - `routes/cookbook_routes.py`: 4 conflict regions.
  - `static/js/cookbook*.js` (5 files): conflict (had local Windows fixes).
- Decided strategy: keep our unique work, port the one semantic conflict.

## 2) Safety net (DONE)
- Created `backup/dev-pre-upstream-merge-20260710` ref before merging.

## 3) Merge + conflict resolution (DONE)
- `git merge upstream/dev` → resolved all conflicts:
  - Imports: merged both sets.
  - `model_download` top: kept our `_check_hf_reachable` pre-flight + upstream `env_prefix`.
  - HF retry loop: **ported our hf_transfer-disable-on-retry into upstream's
    `$ODYSSEUS_HF_CLI` abstraction** (upstream refactored the inline body away).
  - Cached-model scan: took upstream's `_run_cached_scan_once()` refactor.
  - JS (5 files): **kept our versions** (carry local Windows/tmux/HF_HOME fixes).
- Real merge commit `dd617b5`.

## 4) Test repair (DONE)
- Stale JS-snapshot assertions updated to current JS (`combinedOutput` removed,
  `env_path`→`envPath`, zombie recovery via `_downloadSessionTask`). Commit `af4ab77`.
- Session log updated + committed (`83d953a`).

---

## Done criteria
1. ✅ Fork no longer behind upstream (`0` behind, was 44).
2. ✅ Our unique work preserved (Hermes-shutdown + ChromaDB fixes untouched; HF
   pre-flight + retry-fallback ported into upstream structure).
3. ✅ 45/45 cookbook+security tests pass; 115 broader sample pass.
4. ✅ Launcher PS1 files: 0 diff vs pre-merge.
5. ✅ Pushed to `origin/dev` (3 commits: merge, test-repair, log).

## Risk notes
- Our JS is intentionally ahead of upstream's (keeps local Windows fixes); future
  upstream JS changes must be re-ported, not blindly merged.
- Full 4657-test suite not run (timeout); sampled subsets all green. CI run advised.
