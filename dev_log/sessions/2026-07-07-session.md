# 2026-07-07 Session — Windows Launcher Audit, Fixes, and Live Verification

## Overview

Focused session on hardening `launch-windows.ps1` and `reset.ps1`, diagnosing the deterministic `ConnectionResetError(10054)` to public PyPI, and verifying the launcher end-to-end on Windows 11.

## Environment

- **OS:** Windows 11
- **Shell:** Hermes terminal (git-bash/MSYS) + PowerShell
- **Python:** standalone `3.13.14` at `C:\Users\jyang\AppData\Local\Programs\Python\Python313\python.exe`
- **venv:** `C:\Users\jyang\odysseus\venv`
- **Repository:** Odysseus `dev` branch
- **Server port:** `127.0.0.1:7000`

---

## Completed Work

### 1. Launcher script fixes
- Added missing `Test-IsHermesAgentPython` function
- Fixed broken `sitecustomize.py` generation (added `import sys`)
- Applied Fix A–G from `dev_log/plans/2026-07-07-plan.md`
- Added automatic default mirror to `https://pypi.tuna.tsinghua.edu.cn/simple` when `ODYSSEUS_PIP_MIRROR` is unset
- Refactored mirror fallback into `Invoke-PipMirrorFallback` helper
- Added post-install verification gate with `Test-OdysseusDepsReady`
- Made exit-code propagation deterministic with step-scoped `$stepExitCode`
- Fixed `PIP_EXTRA_INDEX_URL` save/restore around mirror logic
- Made launcher window resident until manually closed

### 2. Reset script fixes
- Removed `$ErrorActionPreference = 'SilentlyContinue'`
- Added truthful failure reporting with `$failed` tracking
- Added resident `Read-Host` pause at end

### 3. Live verification
- Confirmed `launch-windows.ps1` reaches uvicorn and serves `HTTP 200` on `127.0.0.1:7000`
- Confirmed dependencies install via mirror without `ConnectionResetError(10054)`
- Confirmed `setup.py` and first-time setup complete successfully

---

## Current commit

- Working toward commit from `ad3642f fix: launch-windows auto-open after readiness; tests conftest import_state`
- Branch: `dev`

---

## Known Open Items

1. `Get-OdysseusProcesses` repo-root fallback can over-match same-checkout MCP servers
2. Public PyPI still 10054 from this host; default mirror bypasses it
3. `pytest` not available in this workspace; verification limited to PowerShell AST/syntax checks and live HTTP probes

---

## Next Steps

- Commit current launcher/reset fixes to `dev`
- Optionally address `Get-OdysseusProcesses` over-broad match
- Optionally add exclusion filter for `mcp_servers/` in process matcher

---

## Session Status

🟢 Progress saved; ready to commit.
