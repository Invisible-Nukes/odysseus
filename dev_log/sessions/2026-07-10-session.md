# 2026-07-10 Session — Upstream Sync Assessment (pewdiepie-archdaemon/odysseus)

## Overview
Assessed how far our fork (`Invisible-Nukes/odysseus`, branch `dev`) has diverged
from upstream (`pewdiepie-archdaemon/odysseus`) and what a merge would entail.
Deliberately did a **dry, abortable merge probe** to ground the conflict plan in
real evidence rather than guessing. No real merge performed yet — this session is
assessment + planning only.

## Environment
- OS: Windows 11; Shell: Hermes terminal (git-bash/MSYS) + PowerShell 5.1
- Repo: Odysseus `dev`; upstream: `pewdiepie-archdaemon/odysseus`
- Model this session: tencent/hy3:free (nous)

---

## Divergence (after `git fetch upstream`)
| Comparison | Ahead (ours) | Behind (theirs) |
|---|---|---|
| `dev` vs `upstream/dev` | **49** | **44** |
| `dev` vs `upstream/main` | 49 | 38 |

- We are **44 commits behind `upstream/dev`**, but also **49 commits ahead** (our
  own local fixes + the earlier "sync 216 commits" merge).
- The 44 upstream commits are predominantly: **security fixes** (SSRF guards on
  webhooks/integrations/email, CodeQL-friendly research lookups, email body
  sanitization, owner scoping), **chat fixes** (explicit web-search enable/deny,
  MIME for extensionless uploads, copilot guard), and **stability/refactor**
  (Cookbook download runner for Python 3.11, contacts subpackage, cookbook serve
  task gating).

## Dry-merge probe (evidence, then aborted)
Ran `git checkout -b _merge_probe origin/dev && git merge --no-commit upstream/dev`
to see real conflicts, then `git merge --abort` + deleted the probe branch.

### Conflicts found
- **`routes/cookbook_routes.py`** — 4 conflict regions.
  1. **Lines ~62-68 (import block):** upstream added new imports
     (`_append_pip_install_runner_lines`, `_pip_install_command_without_break_system_packages`,
     `_diagnose_serve_output`, `run_ssh_command_async`) to the same import tuple we
     customized. Trivially resolved: keep BOTH sets.
  2. **Lines ~1083-1101 (model_download handler top):** we added the
     `_check_hf_reachable` pre-flight; upstream added
     `if req.remote_host and not req.env_prefix: req.env_prefix = _server_env_prefix_for_download(...)`.
     **Both are independent — keep both.**
  3. **Lines ~1349-1376 (HF download retry loop):** OUR core change. We added the
     "disable hf_transfer on retry" fallback. Upstream replaced the whole retry
     body with a single `runner_lines.append(f'  "$ODYSSEUS_HF_CLI" {hf_download_args} < /dev/null')`
     — i.e. upstream refactored the download to use a prebuilt `ODYSSEUS_HF_CLI` /
     `hf_download_args` abstraction. **This is the one real semantic conflict:**
     upstream's refactor supersedes our inline approach, so our retry-fallback must
     be re-expressed in terms of upstream's `$ODYSSEUS_HF_CLI` mechanism (or ported
     into their new structure). Our `_check_hf_reachable` pre-flight stays.
  4. **Lines ~1525-1577 (cached-model scan):** upstream wrapped the scan in a
     `_run_cached_scan_once()` helper and changed return shape. Ours is an older
     inline version. **Upstream's version wins** (structural improvement); ours is
     superseded.
- **`static/js/cookbook*.js` (6 files):** `cookbook.js`, `cookbookDownload.js`,
  `cookbookRunning.js`, `cookbookServe.js`, `cookbook-hwfit.js`. All CONFLICT
  (content). These are UI files we have NOT customized locally — almost certainly
  pure upstream changes. Resolution: **accept upstream/theirs** for these (ours has
  no local edits to preserve). Will verify ours has no local diff before taking theirs.

### Non-conflicting (auto-merged cleanly)
`app.py`, `routes/cookbook_helpers.py`, `src/tools/cookbook.py`,
`static/js/cookbook-diagnosis.js` — these auto-merged.

## Key insight
Our **local value-add** lives in three places:
1. `_check_hf_reachable` pre-flight (handler top) — independent of upstream, survives.
2. Hermes-shutdown process matcher (`launch-windows.ps1` / `reset.ps1`) — NOT touched
   by upstream, no conflict at all.
3. ChromaDB full-package install (`launch-windows.ps1`) — NOT touched by upstream.
4. hf_transfer retry-fallback — **conflicts** with upstream's download refactor; must
   be ported, not blindly kept.

The upstream merge is **safe to do** because our unique work is either conflict-free
(launcher/ChromaDB) or independently-addable (HF pre-flight), with exactly ONE
semantic conflict (the download retry loop) that needs a careful port.

## Session Status
🟡 Assessment complete. Dry-merge aborted; working tree clean. Awaiting go-ahead to
perform the real merge with the conflict-resolution plan below.

---

## Proposed merge plan (for user approval — NOT yet executed)
1. `git fetch upstream` (done) then `git checkout dev && git merge upstream/dev`.
2. Resolve `routes/cookbook_routes.py`:
   - Region 1 (imports): keep both sets.
   - Region 2 (handler top): keep both (pre-flight + env_prefix).
   - Region 3 (HF retry loop): take upstream's `$ODYSSEUS_HF_CLI` refactor, then
     re-apply our "disable hf_transfer on retry" logic inside their structure; keep
     `_check_hf_reachable` pre-flight.
   - Region 4 (cached scan): take upstream's `_run_cached_scan_once()` version.
3. Resolve `static/js/cookbook*.js` (6): confirm no local edits, then accept upstream.
4. Verify: `python -c "import ast; ast.parse(...)"` on the merged .py; re-run
   `tests/test_cookbook_hf_reachable.py` (8 tests); confirm launcher PS1 files
   untouched.
5. Commit the merge; push to `origin/dev`. Do NOT fast-forward; keep a real merge
   commit so the sync is auditable.
6. Update dev_log (2026-07-10 session/plan/analysis) after merge.

## Risk notes
- The 44 upstream commits include security fixes — merging them IN is the safer
  direction than staying behind.
- Only genuine code-judgment conflict is region 3 (download retry). Everything else
  is mechanical (imports) or pure-upstream (JS UI).
- Will NOT delete or rewrite the launcher/ChromaDB fixes — those are conflict-free.
