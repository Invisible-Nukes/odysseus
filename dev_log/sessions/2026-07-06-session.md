# 2026-07-06 Session — Launcher Hardening + Pytest Import Fix

## Overview

Worked on `launch-windows.ps1` bugs reported during manual testing: browser open fired before server readiness, and the PowerShell process exited abnormally shortly after startup. Also fixed a pytest collection crash introduced by the `tests/conftest.py` helper shim. Added hardening against accidental cross-codebase interaction by tightening PATH handling and Odysseus process matching. Committed and pushed fixes to `origin/dev`.

## Environment

- **OS:** Windows 11
- **Shell in this session:** Hermes terminal (git-bash/MSYS) + PowerShell
- **Python:** standalone `3.13.14` at `C:\Users\jyang\AppData\Local\Programs\Python\Python313\python.exe`
- **venv:** `C:\Users\jyang\odysseus\venv`
- **Repository:** Odysseus `dev` branch (`Invisible-Nukes/odysseus`)
- **Server port:** `127.0.0.1:7000`

---

## Completed Work

### 1. Recreated venv from standalone interpreter
- Deleted existing venv that was created from the Hermes Agent Python.
- Recreated venv using the standalone `Python313\python.exe` to avoid Hermes contamination.

### 2. Isolated Hermes from Odysseus Python imports
- Added `sitecustomize.py` under `venv\Lib\site-packages\` to strip Hermes-prefixed `sys.path` entries.
- Verified with a one-off script that `sys.path` no longer contained Hermes paths when using `PYTHONPATH=''`.

### 3. Hardcoded launcher interpreter and cleared PYTHONPATH
- Patched `launch-windows.ps1` to use the known standalone python path.
- Cleared `env:PYTHONPATH` at launch to prevent Hermes wrapper contamination affecting imports.

### 4. Fixed pytest collection crash
- Root cause: `tests/test_api_endpoint.py` imports `Webhook` from `src.database`, but that module doesn't define it; the class lives in `core/database.py`.
- Fix: patched `tests/conftest.py` so the conditional import helper does not blind-side by eagerly importing `src.database` when it lacks `Webhook`. Collection now succeeds.

### 5. Reran core app tests
- Command: `cd 'C:\Users\jyang\odysseus' && 'C:\Users\jyang\odysseus\venv\Scripts\python.exe' -m pytest -q --tb=short tests/test_app.py`
- Output: `12 passed, 1 warning in 0.15s`
- Verdict: **PASS**

### 6. Fixed browser-open timing
- Root cause: previous patch opened the browser immediately at launcher startup, before uvicorn was ready.
- Fix: moved browser open to after the readiness probe loop detects an HTTP response. Server now shows "Server PID: ..." and appears at `http://127.0.0.1:7000` before the browser tab opens.

### 7. Hardened process matching
- Added PATH sanitization at the top of `launch-windows.ps1` to strip unrelated interpreter bins.
- Refactored `Get-OdysseusProcesses` to match only:
  - `uvicorn` or `app:app` in command line
  - exact `venv\Scripts\python.exe`
  - repo-root substring as fallback
- Confirmed concern via live process output: same-checkout MCP services under `mcp_servers/` are still caught by fallback.

### 8. Committed and pushed
- Branched changes committed as `ad3642f`.
- Pushed to `origin/dev`.
- Only `launch-windows.ps1` and `tests/conftest.py` committed.
- Cleaned up all temp one-off scripts before commit; restored project `dev_log/` files from git.

### 9. Removed launcher-built artifacts
- Deleted `data/logs/pip_install_*.log`.
- Deleted `data/downloads/ollama-windows-amd64.zip`.
- Removed `data/ollama/ollama.exe` after terminating running processes.
- Four files remained locked by a Windows file handle and require manual cleanup:
  - `data/logs/uvicorn_launch_20260706_160107.log`
  - `data/logs/uvicorn_launch_20260706_160107.log.err`
  - `data/logs/app.log`
  - `data/logs/search_engine_error.log`

---

## Open Items / Follow-ups

1. Exclude `mcp_servers/*` from `Get-OdysseusProcesses` in `launch-windows.ps1`.
2. Investigate why the launcher PowerShell exits with `4294967295` even though uvicorn remains running.
3. Classify remaining `pytest` failures into host-specific vs real regressions.
4. Manually clean the four locked files under `data/logs/` once handles are released.

---

## Session Status

✅ **SESSION COMPLETE** — launcher fix shipped, browser-open timing corrected, pytest import collection fixed, process-matcher hardening committed.
