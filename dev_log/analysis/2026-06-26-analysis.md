# 2026-06-26 — Launcher ChromaDB & Path Contract Audit (Consolidated)

**Commits:** `22c12d2`, `b258f50` (pip hardening + ChromaDB auto-install/start/stop in `launch-windows.ps1`).
**Verdict:** No critical blockers. Manual E2E confirmed ChromaDB connects at startup; Odysseus serves on `:7000`. **164/164 regression tests passed.**

## `launch-windows.ps1` & ChromaDB audit
### Verified OK
- Pip hardening (`Test-OdysseusDepsReady`, retries, cache purge on `IncompleteRead`).
- Chroma paths align with `src/constants.py` (`CHROMA_DIR` = `{DATA_DIR}/chroma`).
- `CHROMADB_HOST/PORT` defaults `localhost`/`8100` match `src/chroma_client.py`.
- Lifecycle: `Start-OdysseusChromaDb` before uvicorn; `Stop-OdysseusChromaDb` in `finally`.
- Install skip when `chroma.exe`/`pip show chromadb` present.
- macOS parity (same port 8100, same data dir).

### Medium issues
- `chromadb-client` + `chromadb` co-installed (macOS removes client first; Windows keeps both — documented conflict risk).
- No `.env` load in launcher (custom `CHROMADB_*` / `ODYSSEUS_DATA_DIR` ignored unless pre-exported).
- Broad process kill: `Get-OdysseusProcesses` repo-root match kills Chroma/MCP/cookbook children on relaunch.
- Uvicorn orphaned on Ctrl+C (stops Chroma only).
- `CHROMADB_HOST=0.0.0.0` skipped on Windows (treated as remote).
- Logs hardcoded `{repo}\data\logs` even when `ODYSSEUS_DATA_DIR` relocated.
- Misleading log banner (uvicorn output not streamed to launcher console).

### Low issues
- `Write-Host "═" * 70` prints literal `* 70` (needs parentheses).
- Unicode checkmarks may garble in some consoles.
- First Chroma pip install ~1-2 min.

## Cookbook Path Contract Windows audit
- Jun 25 remediation **intact**; `launch-windows.ps1` work does NOT regress path contract.
- `tests/test_cookbook_path_contract_regression.py` 6/6, log-path resolver + helpers pass in 81-test batch.
- Path contract OK across `src/constants.py`, launcher env seeding, route injection, download runners, JS `_defaultHubPath()`, ChromaDB under `data/chroma`.

### Open issues (unchanged)
- **A5-005** diagnosis scans `%USERPROFILE%\.cache` not `defaultHubPath`.
- **A2-10** `_shell_path()` leaves Windows backslashes in bash `export HF_HOME`.
- **hwfit** llamacpp preview hardcodes `$HOME/.cache/huggingface/hub`.
- **llama-cpp serve** Linux-oriented bash bootstrap on `local_windows`; Py3.13 CPU wheel absent → source build failure.

## macOS vs Windows gaps
- Chroma install: macOS removes client + installs full; Windows keeps client.
- Chroma stop: macOS `trap` kill; Windows `finally` + `taskkill /T`.
- Chroma log: macOS `$TMPDIR`; Windows `data/logs/chromadb_*.log`.

*Consolidated from: 2026-06-26-launch-windows-chromadb-audit.md, 2026-06-26-cookbook-path-contract-windows-audit.md*
