# 2026-06-25 — Cookbook Dead-Code, Path & Download Audit (Consolidated)

**Environment:** Windows 11 VM (AMD EPYC 7V12, 28GB, CPU-only), Python 3.13.5, `dev` branch.
**Commits:** `bd7106e` / `8306f24` (remediation), `969bc36` (dead-code strip).
**Scope:** Cookbook download/serve/GGUF flows, path contract, dead-code removal, integration + backend + frontend verification, audit-register reconciliation, Git Bash discovery, repo artifact cleanup.

## 1. Path & Download Audit (Agent 0 coordinator + 5 workstreams)
**Top cross-cutting risks:**
1. JS/UI defaults to Unix `~/.cache/huggingface/hub` while server seeds `DATA_DIR/huggingface`.
2. tmux-centric cancel/probe assumes tmux + `python3`, breaking native Windows.
3. `python3` hardcoded in cache probes → Windows (python-only) misclassifies completed as stopped.
4. WSL split-cache (`add_hf_cache`) documented but unwired in `/api/model/cached`.
5. Regression: MiniMax M3 snapshot fixed in Python but `cookbookServe.js` still hardcoded pewds path.

**Issue register outcome:** 50+ rows. 33 RESOLVED, 22 STILL OPEN, 0 REGRESSED, 6 stale-Open (actually fixed: CB-DL-011/012/013, A2-11, A4-4, A5-003).

**Key OPEN items:** A2-03 (Git Bash hard dependency — primary Windows blocker), A2-10 (backslash paths in Git Bash), A3-04 (non-deterministic GGUF `find | head -1`), A5-005 (diagnosis scans wrong Windows cache root), CB-DL-014/A3-06 (hardcoded MiniMax snapshot hash), CB-DL-019/A3-07 (agent `platform: "linux"` default).

## 2. Dead-Code Removal (`969bc36`) — Verification: PASS, no regressions
- Removed `_ssh`/`_ssh_ps` (zero callers), duplicate/unused imports, unused JS injection params.
- **Backend verification:** 97/97 pytest pass; path contract intact (CB-DL-001..009, A2-01/02/04, A3-01, A4-1 all resolved).
- **Frontend verification:** `node --check` 6 files exit 0; 11 pytest pass; MiniMax now uses `_defaultHubPath()`; no pewds paths in JS.
- **Integration verification:** 270/277 pytest pass (7 failures = Windows-host/test-harness artifacts, not regressions); API smoke `/api/health`, `/api/cookbook/state`, `/api/model/cached` all 200; `.env` ↔ `src/constants.py` alignment 5/5.

## 3. Git Bash Discovery
- `find_bash()`/`find_git()` enhanced: registry probes, derive from `git.exe` on PATH, Scoop/Chocolatey paths. Improved A2-03 discovery; stub `.cmd` fallback still applies if no Git install.

## 4. Repo Artifact Cleanup
- Removed `.pytest_cache/`, 17 `__pycache__/`, 9 stray `.pyc`. `.gitignore` extended for venv/test dirs. `venv.broken/` partially removed (29 locked files remained — access denied).

## 5. ENV-1
- Fresh `.env` created with explicit paths under repo data tree (`ODYSSEUS_DATA_DIR`, `HF_HOME`, etc.).

## Sub-agent audit files (merged)
- **DA-1/DA-2/DA-3/DA-4** — backend routes / agent tools / frontend JS / tests+register dead-code audits for `bd7106e`.
- **Dead-code-merge** — approved removal list (apply in Phase 3).
- **deadcode-cleanup-backend / -frontend / -artifacts** — actual removal execution records.

## Follow-ups (non-blocking)
1. Fix 7 Windows pytest failures (pathToFileURL, stale assertions, `@skipif os.name=="nt"` on GPU mocks).
2. Align hwfit preview to `_defaultHubPath()`; fix A2-10 backslash conversion.
3. Run manual VM smoke with HF token for full download→serve chain.

*Consolidated from: 2026-06-25-cookbook-path-download-audit.md, 2026-06-25-verification-integration.md, 2026-06-25-verification-backend.md, 2026-06-25-verification-frontend.md, 2026-06-25-verification-audit-register.md, 2026-06-25-git-bash-discovery.md, 2026-06-25-deadcode-*.md*
